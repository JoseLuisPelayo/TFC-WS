package org.jpsoft.tfcws.adapter.ws.msg;

public enum MsgType {
    JOIN,
    INITIAL_STATE,
    SUBSCRIBED,
    SNAPSHOT_ZONE,
    ERROR,
    PLAYER_MOVE,
    PLAYER_MOVED,
    PLAYER_LOADED,
    DESPAWN_ENTITIES
}
